# WSL2 Configuration Files

This will setup a minimal development environment which includes neovim, nodejs, oh-my-zsh and tmux.  When running `neovim` use `:PlugInstall` to fetch all of the remote plugins.
